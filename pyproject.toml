[project]
name = "prostate-mri-cancer-detection"
version = "0.1.0"
description = "Research prototype for classifier-first prostate MRI cancer detection."
readme = "README.md"
requires-python = ">=3.10"
dependencies = []

[tool.pytest.ini_options]
testpaths = ["tests"]
pythonpath = ["src"]

[tool.black]
line-length = 100

[tool.ruff]
line-length = 100
target-version = "py310"
